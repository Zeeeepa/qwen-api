# Qwen API Server Configuration
# Copy this file to .env and fill in your credentials

# ============================================
# Qwen Provider Authentication (REQUIRED)
# ============================================
# Get your Qwen account credentials from https://chat.qwen.ai
QWEN_EMAIL=your-email@example.com
QWEN_PASSWORD=your-password-here

# ============================================
# Server Configuration (OPTIONAL)
# ============================================
# Server listening port (default: 8080)
# LISTEN_PORT=8080

# Server host (default: 0.0.0.0)
# HOST=0.0.0.0

# Enable debug logging (default: false)
# DEBUG_LOGGING=true

# ============================================
# Authentication Settings (OPTIONAL)
# ============================================
# Skip API key validation for testing (default: false)
# SKIP_AUTH_TOKEN=false

# Enable anonymous mode - still requires Authorization header (default: true)
# ANONYMOUS_MODE=true

# Custom authentication token (if not using anonymous mode)
# AUTH_TOKEN=sk-your-custom-token

# Path to file containing multiple valid tokens (one per line)
# AUTH_TOKENS_FILE=/path/to/tokens.txt

# ============================================
# Feature Flags (OPTIONAL)
# ============================================
# Enable FlareProx for proxy rotation and load balancing (default: false)
# FLAREPROX_ENABLED=false

# Enable tool/function calling support (default: true)
# TOOL_SUPPORT=true

# Maximum tokens to scan in responses (default: 200000)
# SCAN_LIMIT=200000

# ============================================
# Usage Example
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your credentials
# 3. Start server: python main.py
# 4. Or with custom settings: SKIP_AUTH_TOKEN=true python main.py --port 8081

